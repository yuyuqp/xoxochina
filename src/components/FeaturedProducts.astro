---
const products = [
  {
    id: 1,
    name: 'Cascata Waterfall Faucet',
    category: 'Faucets',
    rating: 4.8,
    reviews: 124,
    badge: 'Bestseller',
    badgeColor: 'gold',
    img: 'https://placehold.co/400x400/f5f0e8/9d7c3a?text=Waterfall+Faucet',
  },
  {
    id: 2,
    name: 'Marble Vessel Sink',
    category: 'Bathroom Sinks',
    rating: 4.9,
    reviews: 87,
    badge: 'New',
    badgeColor: 'blue',
    img: 'https://placehold.co/400x400/f0f0f5/4a5a70?text=Vessel+Sink',
  },
  {
    id: 3,
    name: 'Rainfall Shower Head',
    category: 'Shower Systems',
    rating: 4.7,
    reviews: 203,
    badge: 'Sale',
    badgeColor: 'red',
    img: 'https://placehold.co/400x400/e8f0f5/3a5060?text=Shower+Head',
  },
  {
    id: 4,
    name: 'Freestanding Bathtub',
    category: 'Bathtubs',
    rating: 5.0,
    reviews: 42,
    badge: 'Premium',
    badgeColor: 'dark',
    img: 'https://placehold.co/400x400/f5f0f5/604870?text=Freestanding+Tub',
  },
  {
    id: 5,
    name: 'Matte Black Towel Bar Set',
    category: 'Accessories',
    rating: 4.6,
    reviews: 156,
    badge: 'Sale',
    badgeColor: 'red',
    img: 'https://placehold.co/400x400/f0f0f0/383838?text=Towel+Bar+Set',
  },
  {
    id: 6,
    name: 'Smart Dual-Flush Toilet',
    category: 'Toilets & Bidets',
    rating: 4.8,
    reviews: 65,
    badge: 'New',
    badgeColor: 'blue',
    img: 'https://placehold.co/400x400/f0f5f0/406040?text=Smart+Toilet',
  },
  {
    id: 7,
    name: 'Brushed Gold Mixer Tap',
    category: 'Faucets',
    rating: 4.9,
    reviews: 98,
    badge: 'Bestseller',
    badgeColor: 'gold',
    img: 'https://placehold.co/400x400/f5f2e8/8b7040?text=Mixer+Tap',
  },
  {
    id: 8,
    name: 'Frameless Shower Enclosure',
    category: 'Shower Systems',
    rating: 4.7,
    reviews: 33,
    badge: null,
    badgeColor: null,
    img: 'https://placehold.co/400x400/e8f0f8/38607a?text=Shower+Enclosure',
  },
];
---

<section class="featured" id="featured">
  <div class="container">
    <div class="featured__header">
      <div>
        <span class="section-label">Handpicked Selection</span>
        <h2 class="section-title">Bestselling Products</h2>
      </div>
      <a href="#" class="btn btn-outline">View All Products</a>
    </div>

    <div class="products__grid">
      {products.map((p) => (
        <article class="product-card">
          <a href="#" class="product-card__img-link" aria-label={`View ${p.name}`}>
            {p.badge && (
              <span class={`product-card__badge badge--${p.badgeColor}`}>{p.badge}</span>
            )}
            <img src={p.img} alt={p.name} width="400" height="400" loading="lazy" />
            <div class="product-card__actions">
              <button class="product-action-btn" aria-label="Quick view">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
              </button>
              <button class="product-action-btn" aria-label="Add to wishlist">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>
              </button>
            </div>
          </a>
          <div class="product-card__body">
            <p class="product-card__cat">{p.category}</p>
            <h3 class="product-card__name">
              <a href="#">{p.name}</a>
            </h3>
            <div class="product-card__rating">
              <span class="stars" aria-label={`${p.rating} out of 5`}>
                {[1,2,3,4,5].map((i) => (
                  <svg width="12" height="12" viewBox="0 0 24 24"
                    fill={i <= Math.round(p.rating) ? 'currentColor' : 'none'}
                    stroke="currentColor" stroke-width="1.5">
                    <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
                  </svg>
                ))}
              </span>
              <span class="rating-text">{p.rating} ({p.reviews})</span>
            </div>
          </div>
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .featured {
    padding: 5rem 0;
    background: var(--color-bg-soft);
  }

  .featured__header {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    margin-bottom: 2.5rem;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .products__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.25rem;
  }

  .product-card {
    background: #ffffff;
    border-radius: var(--radius-md);
    overflow: hidden;
    box-shadow: var(--shadow-sm);
    transition: box-shadow var(--transition), transform var(--transition);
  }

  .product-card:hover {
    box-shadow: var(--shadow-lg);
    transform: translateY(-4px);
  }

  .product-card__img-link {
    display: block;
    position: relative;
    aspect-ratio: 1;
    overflow: hidden;
    background: var(--color-bg-soft);
  }

  .product-card__img-link img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.4s ease;
  }

  .product-card:hover .product-card__img-link img {
    transform: scale(1.07);
  }

  .product-card__badge {
    position: absolute;
    top: 0.75rem;
    left: 0.75rem;
    z-index: 1;
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    padding: 0.2rem 0.55rem;
    border-radius: 3px;
  }

  .badge--gold { background: var(--color-accent); color: #fff; }
  .badge--blue { background: #3b82f6; color: #fff; }
  .badge--red  { background: #ef4444; color: #fff; }
  .badge--dark { background: var(--color-primary); color: #fff; }

  .product-card__actions {
    position: absolute;
    top: 0.75rem;
    right: 0.75rem;
    z-index: 1;
    display: flex;
    flex-direction: column;
    gap: 0.45rem;
    opacity: 0;
    transform: translateX(8px);
    transition: opacity var(--transition), transform var(--transition);
  }

  .product-card:hover .product-card__actions {
    opacity: 1;
    transform: translateX(0);
  }

  .product-action-btn {
    background: #fff;
    border: none;
    width: 34px;
    height: 34px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-sm);
    color: var(--color-text-muted);
    transition: background var(--transition), color var(--transition);
  }

  .product-action-btn:hover {
    background: var(--color-primary);
    color: #fff;
  }

  .product-card__body {
    padding: 1.125rem;
  }

  .product-card__cat {
    font-size: 0.68rem;
    font-weight: 600;
    letter-spacing: 0.09em;
    text-transform: uppercase;
    color: var(--color-accent-dark);
    margin-bottom: 0.3rem;
  }

  .product-card__name {
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--color-primary);
    margin-bottom: 0.5rem;
    line-height: 1.35;
  }

  .product-card__name a:hover {
    color: var(--color-accent-dark);
  }

  .product-card__rating {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    margin-bottom: 0.75rem;
  }

  .stars {
    display: flex;
    gap: 1px;
    color: var(--color-accent);
  }

  .rating-text {
    font-size: 0.74rem;
    color: var(--color-text-muted);
  }

  @media (max-width: 1100px) {
    .products__grid { grid-template-columns: repeat(3, 1fr); }
  }

  @media (max-width: 768px) {
    .products__grid { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 480px) {
    .products__grid { grid-template-columns: 1fr; }
  }
</style>
